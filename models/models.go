package models

// import (
// 	"database/sql"
// 	_ "github.com/lib/pq"
// )

// var DB, _ = sql.Open("postgres", "user=postgres password=PASSWORD dbname=DBNAME sslmode=disable")
// defer DB.Close()

// var rows, err = DB.Query("INSERT INTO inouts (EMP_ID, EVENT_CODE, DT, DEVICE_SN) VALUES ($1, $2, $3, $4)", emp_id, event_code, dt, device_sn)
